<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.Sherlock">
        <item name="actionBarStyle">@style/Widget.Styled.ActionBar</item>
        <item name="android:actionBarStyle" tools:targetApi="11">@style/Widget.Styled.ActionBar</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Action Bar -->
    <style name="Widget.Styled.ActionBar" parent="Widget.Sherlock.ActionBar.Solid">
        <!-- Empty for now -->
    </style>

    <!-- Activity -->
    <style name="AddSportActivity" parent="AppTheme">
        <!-- Empty -->
    </style>
    
    <!-- General -->
    <style name="FormHeader" parent="android:Widget.Holo.Light.TextView">
        <item name="android:textAppearance">?android:attr/textAppearanceLarge</item>
    </style>

    <style name="BoardHeader" parent="android:Widget.Holo.Light.TextView">
        <item name="android:textAppearance">@android:style/TextAppearance.Medium</item>
    </style>
</resources>